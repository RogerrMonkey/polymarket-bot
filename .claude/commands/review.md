# Review Module

Use the code-reviewer subagent to do a full review of: $ARGUMENTS

Steps:
1. Use code-reviewer subagent to audit the file(s)
2. Cross-reference any issues with tasks/lessons.md
3. Return a prioritized fix list (CRITICAL → WARNING → SUGGESTION)
4. If CRITICAL issues found, do NOT proceed with other tasks until fixed
